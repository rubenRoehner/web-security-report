\usepackage{graphicx}
\usepackage[x11names]{xcolor}

% easy code typesetting
\usepackage[newfloat]{minted}
\usepackage{xcolor}    % For color definitions


% Define custom colors
\definecolor{bggray}{rgb}{0.95, 0.95, 0.95}
\definecolor{frameblue}{rgb}{0.2, 0.4, 0.6}

% Set global minted style
\setminted{
    frame=single,
    framesep=3mm,
    bgcolor=bggray,
    rulecolor=frameblue,
    fontsize=\footnotesize,
    breaklines=true,
    baselinestretch=1.2
}

\usemintedstyle{friendly}

% get rid of warnings
%\usepackage{scrhack}


% hyperref without ugly boxes around links
\usepackage[pdfusetitle]{hyperref}
\hypersetup{
  colorlinks = true,
  urlcolor   = [rgb]{0.06, 0.15, 0.58},
  citecolor  = [rgb]{0.06, 0.15, 0.58},
  linkcolor  = black
}

% enhanced tables
\usepackage{tabularx}

% parsing parameters
\usepackage{pgfkeys}

% boxes and icons
\usepackage[many]{tcolorbox}
\usepackage{fontawesome5}

% headers and footers
\usepackage{scrlayer-scrpage}

\usepackage{listings}

\input{macros/cvss/cvss}
\input{macros/misc}
\renewcommand{\figurename}{Fig.}
\usepackage[figurename=Abbildung]{caption}

\newenvironment{code}{\captionsetup{type=listing}}{}


